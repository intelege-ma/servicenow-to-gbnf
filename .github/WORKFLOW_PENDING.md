# CI Workflow

The GitHub Actions CI workflow file needs to be added at `.github/workflows/ci.yml`.
The current GitHub token lacks the `workflows` scope required to commit workflow files.

Please add the workflow file manually with the content from the CHANGELOG.
